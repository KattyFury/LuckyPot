# Register an app at https://dashboard.privy.io (free tier) and paste its App ID here.
VITE_PRIVY_APP_ID=

# Filled in after `npm run deploy:arcTestnet` in /contracts (the LuckyStackerPoolProxy address).
VITE_POOL_ADDRESS=
